#include<stdio.h>
void sum(int);
int main()
{
	int n;
	printf("Enter the number upto which you want the sum of:");
	scanf("%d",&n);
	sum(n);
	return 0;
}
void sum(int n)
{
	int s=n*(n+1)/2;
	printf("the sum is:%d",s);
}
