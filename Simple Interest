#include<stdio.h>
void SI();
int main()
{
	SI();
	return 0;
}
void SI()
{
	int p,t,r,si;
	printf("enter principal:");
	scanf("%d",&p);
	printf("enter time:");
	scanf("%d",&t);
	printf("enter rate:");
	scanf("%d",&r);
	si=p*t*r/100;
	printf("Simple intrest is: %d",si);
}
